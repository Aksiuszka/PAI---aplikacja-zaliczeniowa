@model IEnumerable<info_2022.Models.Pytanko>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Imie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tresc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Odpowiedz)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Imie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tresc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Odpowiedz)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPytanie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPytanie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPytanie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
